# YouTube Shorts AI Generator — Dependencies
# Install: pip install -r requirements.txt

edge-tts>=6.1.0        # Microsoft Edge TTS (free, multi-voice)
nest-asyncio>=1.5.0     # Enables async in notebooks/scripts
tqdm>=4.60.0            # Progress bars
Pillow>=9.0.0           # Image processing (hook overlays)
requests>=2.28.0        # Pexels API + downloads
librosa>=0.10.0         # Audio analysis for frame-perfect subtitle sync
soundfile>=0.12.0       # Audio file I/O for librosa
python-dotenv>=0.19.0   # Environment variable loading from .env

# NOTE: FFmpeg must be installed separately!
# Windows: winget install ffmpeg   OR  download from https://ffmpeg.org
# Mac:     brew install ffmpeg
# Linux:   sudo apt install ffmpeg
